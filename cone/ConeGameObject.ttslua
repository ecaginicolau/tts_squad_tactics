require("tts_squad_tactics.common.Utils")
require("tts_squad_tactics.common.Constant")
require("tts_squad_tactics.cone.Cone")

function onLoad(saved_data)
end

-- callable
function Cone_create(params)
    Cone.create(table.unpack(params))
end

function onUpdate()
    -- refresh the cone if needed
    Cone.update()
end
